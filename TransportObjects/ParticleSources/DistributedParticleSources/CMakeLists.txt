# Add Source Files to the global list
add_subdirectory(DistributedCEParticleSources)
add_subdirectory(DistributedMGParticleSources)

add_sources(
             ./distributedSource_inter.f90
            )